#!/usr/bin/env bash
# Fullscreen screenshot → ~/Pictures/Screenshots/
DIR="$HOME/Pictures/Screenshots"
mkdir -p "$DIR"
FILE="$DIR/$(date +%Y%m%d_%H%M%S).png"
grim "$FILE"
notify-send "✦ screenshot" "Saved to $FILE" -t 2000
